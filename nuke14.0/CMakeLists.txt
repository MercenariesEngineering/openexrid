
SET(NDK_VERSION 14.0)
SET(NDK_PATH ${NUKE140_LIBRARY_DIR})
SET(NDK_LIBRARIES ${DDIMAGE140_LIBRARY})

ADD_DEFINITIONS(-DNOMINMAX)
ADD_DEFINITIONS(-D_USE_MATH_DEFINES)

INCLUDE(${CMAKE_CURRENT_LIST_DIR}/../nuke/nuke.cmake)
